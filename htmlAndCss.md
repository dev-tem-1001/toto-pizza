# ГАЙД-ШАПАРГАЛКА: HTML/CSS 

## **CSS**

### **1. `display` — как элемент отображается**
```css
display: block;      /* Каждый с новой строки (div, h1, p) */
display: inline;     /* В строку (span, a, strong) */
display: inline-block; /* В строку, но с размерами */
display: flex;       /* Гибкое расположение (современно!) */
display: grid;       /* Сетка (для твоих карточек) */
```

### **2. Отступы: `margin` и `padding`**
```css
margin: 10px;           /* ВНЕШНИЙ отступ со всех сторон */
margin: 10px 20px;      /* верх/низ 10px, лево/право 20px */
margin-top: 10px;       /* Только сверху */

padding: 15px;          /* ВНУТРЕННИЙ отступ */
padding: 10px 20px 30px 40px; /* верх, право, низ, лево */
```

### **3. Размеры: `width` и `height`**
```css
width: 100%;           /* 100% от родителя */
width: 300px;          /* Фиксированная ширина */
width: 50vw;           /* 50% от ширины экрана */

height: auto;          /* Автоматическая высота */
height: 200px;         /* Фиксированная высота */
min-height: 100px;     /* Минимальная высота */
```

### **4. Выравнивание текста**
```css
text-align: left;      /* По левому краю (по умолчанию) */
text-align: center;    /* По центру */
text-align: right;     /* По правому краю */
text-align: justify;   /* По ширине */

line-height: 1.5;      /* Межстрочный интервал */
```

### **5. Цвета и фон**
```css
color: #333;           /* Цвет текста */
color: rgb(51, 51, 51); /* RGB */
color: white;          /* Имя цвета */

background-color: #f0f0f0; /* Цвет фона */
background: linear-gradient(to right, red, blue); /* Градиент */
```

### **6. Границы `border`**
```css
border: 1px solid #ccc;     /* Тонкая серая рамка */
border: 2px dashed red;     /* Пунктирная красная */
border-radius: 10px;        /* Закруглённые углы */
border-radius: 50%;         /* Круг */
```

### **7. Flexbox (выучи это первым!)**
```css
.container {
    display: flex;
    justify-content: center; /* Выравнивание по горизонтали */
    align-items: center;     /* Выравнивание по вертикали */
    flex-direction: row;     /* Направление (row, column) */
    gap: 10px;               /* Расстояние между элементами */
}
```

### **8. Grid (для сеток как у тебя)**
```css
.container {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr; /* 3 колонки поровну */
    grid-template-columns: repeat(4, 1fr); /* 4 колонки */
    gap: 20px;               /* Отступы между ячейками */
}
```

### **9. Позиционирование `position`**
```css
position: static;      /* По умолчанию */
position: relative;    /* Относительное (можно сдвигать) */
position: absolute;    /* Абсолютное (относительно родителя) */
position: fixed;       /* Фиксированное (при прокрутке на месте) */

top: 10px;            /* Отступ сверху */
left: 20px;           /* Отступ слева */
```

### **10. Шрифты `font`**
```css
font-family: Arial, sans-serif; /* Семейство шрифтов */
font-size: 16px;      /* Размер */
font-weight: bold;    /* Жирность (normal, bold) */
font-style: italic;   /* Курсив */
```

---

## **HTML**

### **1. Структурные теги**
```html
<div>...</div>           <!-- Блок-контейнер (display: block) -->
<span>...</span>         <!-- Строчный контейнер (display: inline) -->
```

### **2. Заголовки (важны для SEO)**
```html
<h1>Главный заголовок</h1>   <!-- Только ОДИН на странице! -->
<h2>Подзаголовок</h2>
<h3>Меньший заголовок</h3>
```

### **3. Текст**
```html
<p>Абзац текста</p>
<strong>Важный текст (жирный)</strong>
<em>Выделенный текст (курсив)</em>
<br>                    <!-- Перенос строки -->
```

### **4. Ссылки и кнопки**
```html
<a href="/page">Текст ссылки</a>
<a th:href="@{/page}">Ссылка в Thymeleaf</a>

<button onclick="...">Кнопка</button>
<input type="submit" value="Отправить">
```

### **5. Изображения**
```html
<img src="/images/photo.jpg" alt="Описание">
<img th:src="@{/images/photo.jpg}" th:alt="${name}">
<!-- alt ОБЯЗАТЕЛЕН для доступности! -->
```

### **6. Списки**
```html
<ul>                    <!-- Маркированный список -->
    <li>Пункт 1</li>
    <li>Пункт 2</li>
</ul>

<ol>                    <!-- Нумерованный список -->
    <li>Первый</li>
    <li>Второй</li>
</ol>
```

### **7. Формы (для заказов)**
```html
<form th:action="@{/order}" method="post">
    <input type="text" name="username" placeholder="Имя">
    <input type="email" name="email">
    <textarea name="message"></textarea>
    <select name="size">
        <option value="small">Маленькая</option>
        <option value="large">Большая</option>
    </select>
</form>
```

### **8. Таблицы (для админки)**
```html
<table>
    <tr>                <!-- Строка -->
        <th>Заголовок</th> <!-- Ячейка заголовка -->
        <th>Цена</th>
    </tr>
    <tr>
        <td>Пепперони</td> <!-- Обычная ячейка -->
        <td>550 ₽</td>
    </tr>
</table>
```

### **9. Мета-теги (в <head>)**
```html
<meta charset="UTF-8">           <!-- Кодировка -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- ↑ ОБЯЗАТЕЛЬНО для мобильных! -->
<title>Название страницы</title>
```

### **10. Подключение CSS/JS**
```html
<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" th:href="@{/css/style.css}">
<script src="/js/script.js"></script>
```

---

## 🎯 **ПРАВИЛА ВЕРСТКИ НА ВСЮ ЖИЗНЬ**

### **1. Блочная модель (Box Model)**
```
┌─────────────────────────┐
│        margin           │
│   ┌─────────────────┐   │
│   │     border      │   │
│   │  ┌───────────┐  │   │
│   │  │  padding  │  │   │
│   │  │ ┌───────┐ │  │   │
│   │  │ │ content│ │  │   │
│   │  │ └───────┘ │  │   │
│   │  └───────────┘  │   │
│   └─────────────────┘   │
└─────────────────────────┘
```

### **2. Порядок свойств CSS (читаемость)**
```css
.selector {
    /* 1. Позиционирование */
    position: relative;
    top: 0;
    left: 0;
    
    /* 2. Блочная модель */
    display: block;
    width: 100%;
    margin: 0;
    padding: 10px;
    
    /* 3. Типографика */
    font-family: Arial;
    font-size: 16px;
    color: #333;
    
    /* 4. Оформление */
    background: #fff;
    border: 1px solid #ccc;
    border-radius: 5px;
    
    /* 5. Анимации */
    transition: all 0.3s;
}
```

### **3. Именование классов (БЭМ)**
```css
/* Блок */
.pizza-card {}
/* Элемент */
.pizza-card__image {}
.pizza-card__title {}
/* Модификатор */
.pizza-card--highlighted {}
.pizza-card__button--disabled {}
```

### **4. Адаптивность (mobile-first)**
```css
/* Сначала стили для телефона */
.pizza-card {
    width: 100%;
    margin-bottom: 20px;
}

/* Потом для планшета */
@media (min-width: 768px) {
    .pizza-card {
        width: 48%;
        float: left;
    }
}

/* Потом для компьютера */
@media (min-width: 1024px) {
    .pizza-card {
        width: 23%;
    }
}
```

### **5. Thymeleaf + HTML**
```html
<!-- Цикл -->
<div th:each="item : ${items}"></div>

<!-- Условия -->
<div th:if="${user != null}"></div>
<div th:unless="${user.admin}"></div>

<!-- Вставка переменных -->
<h2 th:text="${pizza.name}"></h2>
<img th:src="@{${pizza.imageUrl}}">

<!-- Ссылки -->
<a th:href="@{/page/{id}(id=${pizza.id})}"></a>
```

---

## 🚀 **АЛГОРИТМ ВЕРСТКИ ЛЮБОЙ СТРАНИЦЫ:**

### **Шаг 1: HTML-скелет**
```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Страница</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>Шапка</header>
    <main>Основной контент</main>
    <footer>Подвал</footer>
</body>
</html>
```

### **Шаг 2: CSS сброс**
```css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box; /* Важно! */
}
```

### **Шаг 3: Контейнеры**
```css
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}
```
